{
  "name": "@midnight-ntwrk/bboard-ui",
  "version": "0.1.0",
  "author": "IOG",
  "license": "MIT",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf ./dist && tsc && vite build --mode preprod && cp -r ../contract/src/managed/donor-proof/keys ./dist/keys && cp -r ../contract/src/managed/donor-proof/zkir ./dist/zkir",
    "build:preview": "rm -rf ./dist && tsc && vite build --mode preview && cp -r ../contract/src/managed/donor-proof/keys ./dist/keys && cp -r ../contract/src/managed/donor-proof/zkir ./dist/zkir",
    "build:start": "npm run build && npm run start",
    "build:start:preview": "npm run build:preview && npm run start",
    "ci": "npm run typecheck && npm run lint",
    "dev": "vite",
    "dev:preprod": "vite --mode preprod",
    "dev:preview": "vite --mode preview",
    "lint": "eslint ./src",
    "start": "http-server --port 0 ./dist",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "preview": "vite preview",
    "prepack": "yarn build"
  },
  "dependencies": {
    "@emotion/react": "^11.14.0",
    "@emotion/styled": "^11.14.0",
    "@mui/icons-material": "^7.3.9",
    "@mui/material": "^7.3.8",
    "node-stdlib-browser": "^1.3.1",
    "react": "^19.2.4",
    "react-dom": "^19.2.4",
    "react-router-dom": "7.14.0"
  },
  "devDependencies": {
    "@swc/core": "^1.15.11",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.1",
    "eslint-plugin-react": "^7.37.5",
    "vite-plugin-node-polyfills": "^0.26.0"
  }
}
